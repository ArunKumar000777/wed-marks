import React from "react";
import Hero from "../components/Hero";

const Home = () => {
    return (
        <div className="px-40 bg-accent_primary">
            <Hero />
        </div>
    );
};

export default Home;
